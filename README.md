# 🚀 Telegram Bot - Быстрый запуск

## Мгновенная установка и запуск

### Вариант 1: Bash скрипт (рекомендуется)
```bash
# Скачайте проект и запустите
./quick_start.sh
```

### Вариант 2: Python скрипт
```bash
python3 setup.py
```

## Что нужно для запуска

1. **Токен бота** - получите от [@BotFather](https://t.me/BotFather)
2. **Ваш Telegram ID** - получите от [@userinfobot](https://t.me/userinfobot)

## Что делает автоматически

✅ **Проверяет зависимости** и устанавливает недостающие  
✅ **Создает виртуальное окружение**  
✅ **Устанавливает все пакеты** (aiogram, aiosqlite, и др.)  
✅ **Тестирует подключение** к боту  
✅ **Создает конфигурацию** (.env файл)  
✅ **Настраивает автозапуск** (systemd сервис)  
✅ **Запускает бота** в режиме 24/7  

## Ограничения

⏰ **Запуск только раз в сутки** - защита от случайного повторного запуска

## Управление ботом

### Запуск/остановка
```bash
# Статус
sudo systemctl status telegram-bot

# Запуск
sudo systemctl start telegram-bot

# Остановка
sudo systemctl stop telegram-bot

# Перезапуск
sudo systemctl restart telegram-bot
```

### Просмотр логов
```bash
# Живые логи
tail -f logs/bot.log

# Логи сервиса
sudo journalctl -u telegram-bot -f
```

### Ручной запуск
```bash
# Активация окружения и запуск
source venv_bot/bin/activate
python3 main.py
```

## Структура проекта

```
telegram-bot/
├── main.py              # Основной файл бота
├── setup.py             # Python установочный скрипт
├── quick_start.sh       # Bash установочный скрипт
├── test_bot.py          # Тестовый бот
├── .env                 # Конфигурация (создается автоматически)
├── venv_bot/           # Виртуальное окружение
├── logs/               # Логи
├── data/               # База данных
├── config/             # Конфигурационные файлы
└── README.md           # Эта инструкция
```

## Функции бота

🤖 **Учет персонала** - отметка прибытия/убытия  
📊 **Статистика** - отчеты по присутствию  
🔔 **Уведомления** - автоматические напоминания  
🛠️ **Админ-панель** - управление пользователями  
📱 **Telegram интерфейс** - удобные кнопки  

## Быстрый старт

1. Получите токен бота у [@BotFather](https://t.me/BotFather)
2. Узнайте свой ID у [@userinfobot](https://t.me/userinfobot)
3. Запустите: `./quick_start.sh`
4. Введите токен и ID
5. Ждите 30 секунд
6. Готово! Бот работает 24/7

## Решение проблем

### Нет прав на создание сервиса
```bash
sudo ./quick_start.sh
```

### Ошибка подключения к боту
- Проверьте правильность токена
- Убедитесь, что бот не заблокирован
- Проверьте интернет соединение

### Бот не отвечает
```bash
# Проверьте статус
sudo systemctl status telegram-bot

# Перезапустите
sudo systemctl restart telegram-bot

# Посмотрите логи
tail -f logs/bot.log
```

## Обновление

```bash
# Остановите бота
sudo systemctl stop telegram-bot

# Обновите код
git pull

# Запустите снова
sudo systemctl start telegram-bot
```

---

**🎉 Готово! Ваш бот работает 24/7 и готов к использованию!**

Найдите бота в Telegram и отправьте команду `/start`